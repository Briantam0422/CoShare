<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fyp2">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".order.orderRequestRecord.orderRequestRecord"></activity>
        <activity android:name=".order.orderRequest.orderRequestRecord" />
        <activity android:name=".order.orderRequest.orderRequest" />
        <activity android:name=".Search.ShowRequestProductInformation" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Search.RequestDisplayMapsActivity"
            android:label="@string/title_activity_request_display_maps" />
        <activity
            android:name=".Search.SeachRequestInformation"
            android:label="@string/title_activity_seach_request_information" />
        <activity android:name=".Search.SearchMainPage" />
        <activity android:name=".order.orderHelp.orderHelp" />
        <activity android:name=".order.orderUpload.orderUpload" />
        <activity android:name=".order.orderRequest" />
        <activity android:name=".order.orderSearch.orderSearch" />
        <activity android:name=".order.order" />
        <activity android:name=".MAP.ShowProductInformation" />
        <activity
            android:name=".MAP.DisplayMapsActivity"
            android:label="@string/title_activity_display_maps" />
        <activity android:name=".MAP.ListOfPosition" />
        <activity
            android:name=".MAP.CurrentLocationActivity"
            android:label="@string/title_activity_current_location" />
        <activity android:name=".MAP.MapProductInformation" />
        <activity android:name=".UserInfromationChange" />
        <activity android:name=".Supply.UpLoadProductInformation" />
        <activity android:name=".Supply.supplyActivity" />
        <activity android:name=".CustomerInformation" />
        <activity android:name=".ChatLogActivity.Chat_from_row" />
        <activity android:name=".menu.UserAccountActivity" />
        <activity android:name=".ChatLogActivity.ChatLogActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".menu.message_page" />
        </activity>
        <activity android:name=".ChatLogActivity.SearchUser">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".menu.message_page" />
        </activity>
        <activity android:name=".DashboardActivity" />
        <activity android:name=".menu.message_page" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" />
        <activity android:name=".registration" />
        <activity android:name=".menu.save" />
        <activity android:name=".menu.Setting" />
    </application>

</manifest>